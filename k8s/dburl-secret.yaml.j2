---
apiVersion: v1
kind: Secret
metadata:
  name: dburl
  namespace: dirtviz
type: Opaque
stringData:
  DB_USER: "{{ env['DB_USER'] }}"
  DB_PASS: "{{ env['DB_PASS'] }}"
  DB_HOST: "{{ env['DB_HOST'] }}"
  DB_PORT: "{{ env['DB_PORT'] }}"
