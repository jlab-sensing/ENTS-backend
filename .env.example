docker compose up --build -d
# See README.md for more information on the purpose of each variable and how to
# generate the values for each variable.

# Flask Configuration
CONFIG_TYPE=Production

# Postgres Env
DB_USER=dirtviz
DB_PASS=password
DB_HOST=postgresql
DB_PORT=5432
DB_DATABASE=dirtviz

# Domain alias leave blank if not needed
PUBLIC_URL=http://localhost:3000

# Celery configuration
# See the following for URL formats based on the broker/backend you are using.
# https://docs.celeryq.dev/en/stable/getting-started/backends-and-brokers/index.html
CELERY_BROKER_URL=redis://valkey:6379/0
CELERY_RESULT_BACKEND=redis://valkey:6379/0

# Google OAuth credentials
# Allows for logins through with google accounts.
# See README.md to generate the values through the google cloud console.
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# Flask Encryption
# Regenerate for production. See README.md for generating a secret key
SECRET_KEY=eWlAZBMkE86Al_kZ0Uh2pw

# Tokens for auth module
# Regenerate for production. See README.md for generating secrets keys
REFRESH_TOKEN_SECRET=ZxgJO5ZMTX9FRvyHLbIjuw
ACCESS_TOKEN_SECRET=0dyZbKf-C5ETM_gkBpznHQ
ACCESS_TOKEN_AUDIENCE=
ACCESS_TOKEN_ISSUER=

# API rate limiting
# RATE_LIMIT_TRUSTED_PROXY_COUNT should match the number of reverse proxies
# in front of the app. Keep 0 unless ProxyFix should trust X-Forwarded-For.
RATE_LIMIT_ENABLED=true
RATE_LIMIT_STORAGE_URI=redis://valkey:6379/1
RATE_LIMIT_TRUSTED_PROXY_COUNT=0
RATE_LIMIT_DEFAULT_CAPACITY=300
RATE_LIMIT_DEFAULT_REFILL_RATE=5
RATE_LIMIT_HEAVY_READ_CAPACITY=120
RATE_LIMIT_HEAVY_READ_REFILL_RATE=2
RATE_LIMIT_INGEST_CAPACITY=600
RATE_LIMIT_INGEST_REFILL_RATE=10
RATE_LIMIT_EXPORT_CAPACITY=5
RATE_LIMIT_EXPORT_REFILL_RATE=0.033
RATE_LIMIT_POLL_CAPACITY=30
RATE_LIMIT_POLL_REFILL_RATE=1
RATE_LIMIT_AUTH_TOKEN_CAPACITY=10
RATE_LIMIT_AUTH_TOKEN_REFILL_RATE=0.167
RATE_LIMIT_AUTH_GENERAL_CAPACITY=30
RATE_LIMIT_AUTH_GENERAL_REFILL_RATE=0.5

# Testing DB
# This values overwrites the value of SQLALCHEMY_DATABASE_URI in the test
# environment. This is useful for testing the application with a different
# database than the one used in production.
TEST_SQLALCHEMY_DATABASE_URI=

TTN_API_KEY=NNSXS.7NXWV2GJJHJIC2WHI3MIPQYSRIC5AIP36UJNWYQ.S5YCBEAB7DHITEFI3EMRVRCMHGFE5SEWWMJTSBHFXBYJID7RQDNA
TTN_APP_ID=soil-power-sensor
